if (window.history.pushState!=null) window.history.pushState(null, 'Route Guides | Routes', '/routes/<%=@id%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'routes/show_many')).html_safe %>");

