if (window.history.pushState!=null) window.history.pushState(null, 'Route Guides | Forums', '/forums/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'forums/show')).html_safe %>");
