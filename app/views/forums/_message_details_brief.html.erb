           <p>
             <b> <%= @show_message.subject.capitalize%> </b>
             <%=link_to @count.to_s+" messages - Latest: "+@show_message.created_at.strftime("%Y/%m/%d %H:%M"), '/forums/1/?thread='+URI.escape(@show_message.subject), remote: true%>

           </p>


     <%if (signed_in? and @show_message.fromUser.id == @current_user.id)%>
          <p class="right-bubble-blue">
     <%else%>
          <p class="right-bubble">
     <% end %>
            <b><%= @show_message.fromUser.name.capitalize+" >" %></b><br/>
            <% message=@show_message.message.html_safe %>
            <% if message.length>255 then  %>
                  <%= message[0..245].html_safe %>
                  <%= link_to "... (more)", '/forums/1/?thread='+URI.escape(@show_message.subject), remote: true%>
            <% else %>
                  <%= message.html_safe %>
            <% end %>
            
          </p>

