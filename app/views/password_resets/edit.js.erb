if (window.history.pushState!=null) window.history.pushState(null, 'Route Guides', '/password_resets/edit');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'password_resets/edit')).html_safe %>");

