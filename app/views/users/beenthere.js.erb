if (window.history.pushState!=null) window.history.pushState(null, '', '/users/<%=@user.name%>/beenthere');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'users/beenthere')).html_safe %>");


