<% provide(:title, @user.name) %>
<% @title=" | User | "+@user.name %>
<%= render 'flash' %>

<script> 
   update_title('<%=@title%>'); 
   document.getElementById("page_status").innerHTML = '';
</script>
User:  <%= @user.name %><br/>
<%     if((@user.id==@current_user.id) or (@current_user.role==Role.find_by( :name => 'root'))) %>
  Email: <%= @user.email %><br/>
  <%=  link_to "Edit", '/users/'+@user.id.to_s+'/edit', class: "btn btn-large btn-primary", remote: true %>
<%end%>
