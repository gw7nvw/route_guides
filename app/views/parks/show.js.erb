if (window.history.pushState!=null) window.history.pushState(null, '', '/parks/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'parks/show')).html_safe %>");


