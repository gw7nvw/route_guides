if (window.history.pushState!=null) window.history.pushState({stt: "show"}, 'Route Guides | Photos', '/photos/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'photos/show')).html_safe %>");
