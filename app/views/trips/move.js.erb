if (window.history.pushState!=null) window.history.pushState(null, 'Route Guides | Trips', '/trips/<%=params[:id]%>/move');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'trips/move')).html_safe %>");
