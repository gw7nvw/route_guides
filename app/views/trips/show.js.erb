if (window.history.pushState!=null) window.history.pushState(null, 'Route Guides | Trips', '/trips/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'trips/show')).html_safe %>");
